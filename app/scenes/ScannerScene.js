import React, {Component} from 'react';
import {Text, StyleSheet, View} from 'react-native';

export default class ScannerScene extends Component {
  render() {
    return (
      <View>
        <Text> Scanner Scene </Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
